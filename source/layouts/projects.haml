= wrap_layout :layout do
  .container
    .page-header
      %h1 Projects

    .row
      .col-md-3= partial "projects/menu"
      .col-md-9
        .page-header.first-heading
          %h2= current_page.data.project || "Overview"

        - if !current_page.data.project.nil?
          = partial "projects/info"

        = find_and_preserve do
          = yield
